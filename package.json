{
  "name": "react-native-percentage-reactangle",
  "version": "1.0.1",
  "description": "This is to show the percentage bar around the reactangle with which we can use image inside it or the percentage count inside it",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "abhishek_kumar",
  "license": "ISC",
  "peerDependencies": {
    "react": "*",
    "react-native": "*"
  }
}
